---
sidebar_label: 'ROS 2 Fundamentals'
title: 'ROS 2 Fundamentals'
---

# ROS 2 Fundamentals

## Introduction to Robot Operating System 2

Welcome to the ROS 2 Fundamentals chapter! This chapter will introduce you to the Robot Operating System 2 (ROS 2), which is a flexible framework for writing robot software. ROS 2 is a middleware that provides services designed for a heterogeneous computer cluster, including hardware abstraction, device drivers, libraries, visualizers, message-passing, package management, and more.

## What is ROS 2?

ROS 2 is the next generation of the Robot Operating System. It addresses many of the limitations of the original ROS, particularly around:

- **Real-time support**: Better real-time capabilities for time-critical applications
- **Multi-robot systems**: Improved support for multiple robots working together
- **Commercial product support**: More suitable for production and commercial applications
- **Security**: Built-in security features for safe deployment
- **Quality of Service (QoS)**: Configurable communication behavior for different use cases

## Key Concepts

### Nodes

A node is an executable that uses ROS 2 to communicate with other nodes. In ROS 2, nodes are the fundamental building blocks of a ROS system.

```js
// Example of a simple ROS 2 node in Python
import rclpy
from rclpy.node import Node

class MinimalPublisher(Node):

    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello World: %d' % self.i
        self.publisher_.publish(msg)
        self.get_logger().info('Publishing: "%s"' % msg.data)
        self.i += 1
```

<CodeExample
  title="Simple ROS 2 Publisher"
  language="python"
  description="This example shows how to create a simple publisher node in ROS 2."
  expectedOutput="Publishing: 'Hello World: 0', Publishing: 'Hello World: 1', etc.">
```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MinimalPublisher(Node):

    def __init__(self):
        super().__init__('minimal_publisher')
        self.publisher_ = self.create_publisher(String, 'topic', 10)
        timer_period = 0.5  # seconds
        self.timer = self.create_timer(timer_period, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = 'Hello World: %d' % self.i
        self.publisher_.publish(msg)
        self.get_logger().info('Publishing: "%s"' % msg.data)
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    minimal_publisher = MinimalPublisher()
    rclpy.spin(minimal_publisher)
    minimal_publisher.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```
</CodeExample>

### Topics and Messages

Topics are named buses over which nodes exchange messages. A node can publish messages to a topic or subscribe to a topic to receive messages.

### Services

Services provide a request/response communication pattern, which is useful for tasks that require a direct response.

<CodeExample
  title="Simple ROS 2 Service Client"
  language="python"
  description="This example shows how to create a service client in ROS 2."
  expectedOutput="Result of add_two_ints: 5">
```python
import rclpy
from rclpy.node import Node
from example_interfaces.srv import AddTwoInts

class MinimalClientAsync(Node):

    def __init__(self):
        super().__init__('minimal_client_async')
        self.cli = self.create_client(AddTwoInts, 'add_two_ints')
        while not self.cli.wait_for_service(timeout_sec=1.0):
            self.get_logger().info('service not available, waiting again...')
        self.req = AddTwoInts.Request()

    def send_request(self):
        self.req.a = 2
        self.req.b = 3
        self.future = self.cli.call_async(self.req)
        rclpy.spin_until_future_complete(self, self.future)
        return self.future.result()

def main():
    rclpy.init()

    minimal_client = MinimalClientAsync()
    response = minimal_client.send_request()
    minimal_client.get_logger().info(
        'Result of add_two_ints: %d' % response.sum)

    minimal_client.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```
</CodeExample>

## Working with ROS 2

### Installation

ROS 2 can be installed on Ubuntu, Windows, and macOS. The latest release is ROS 2 Humble Hawksbill, which is an LTS (Long Term Support) release.

### Creating a Workspace

ROS 2 uses workspaces to organize your projects:

```bash
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws
colcon build
source install/setup.bash
```

## Practical Exercise

<ProgressTracker chapterId="ros2-fundamentals" chapterTitle="ROS 2 Fundamentals" />

Now that you've learned about ROS 2 fundamentals, try creating your own simple publisher node that publishes a custom message at 1 Hz. The message should contain your name and a counter that increments with each publication.

<div class="admonition admonition-hint alert alert--secondary">
  <div class="admonition-heading">
    <h5>Hint</h5>
  </div>
  <div class="admonition-content">
    <ol>
      <li>Create a new package: <code>ros2 pkg create --build-type ament_python my_publisher</code></li>
      <li>Create the node file in <code>my_publisher/my_publisher/publisher_node.py</code></li>
      <li>Use the rclpy library to create a publisher</li>
      <li>Create a timer to publish messages at 1 Hz (every second)</li>
      <li>Don't forget to initialize rclpy and spin the node</li>
    </ol>
  </div>
</div>

## Summary

In this chapter, you learned about:

- The fundamentals of ROS 2 and how it differs from ROS 1
- Key concepts like nodes, topics, messages, and services
- How to create simple publisher and service client nodes
- The basics of working with ROS 2 workspaces

Understanding these fundamentals is crucial for working with more advanced robotics applications, including those involving simulation environments like Gazebo and Isaac Sim.

## Next Steps

After completing this chapter, you should be ready to explore:

- [Gazebo Simulation](../chapters/gazebo-simulation)
- [Unity Integration](../chapters/unity-integration)
- [Isaac Sim](../chapters/isaac-sim)

<ChapterNavigationWrapper currentChapterId="ros2-fundamentals" />